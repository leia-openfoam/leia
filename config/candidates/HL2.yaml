# CANDIDATE HL2 (halo-limited dossier): HL0 plus the soft wall of S1.
# QUESTION: band protection where HL1 would need a large mu.
# PRE-REGISTERED READ-OUT (2026-09-26, before any run):
#   prediction: the gradient band error is at most that of HL0 in every arm; the wall is
#     weak where the extension already holds q near 1 (its rate follows the strain of the
#     extended velocity, which is small near the interface).
#   pass: the gate verdict AND the target below.
#   falsified if: the shear-arm gradient band error at the finest rung is above 0.8 x the
#     baseline, or a volume or shape metric regresses beyond the tolerance.
candidate: HL2
line: semiLagrangian
description: "halo-limited extension + soft wall S1a"
tokens:
  VELOCITY_EXTENSION: haloLimited
  SL_TRACE_FLUX: extension
  HL_RADIUS_CELLS: 1
  HL_M: 2
  HL_BETA: 1
  HL_DIRECTION: levelSet
  HL_SAMPLER: stencilFit
  SL_SOURCE: gradientControl
  SL_SOURCE_BAND_CELLS: 3
  GC_LAW: softWall
  GC_STRAIN_WEIGHT: none
  SW_C_KAPPA: 1.25
  SW_DELTA_S: 0.08
  SW_P: 5
  SW_GAMMA: 1.4722194895832
  SW_EPS_D: 0
target:
  metric: gradientBandError
  ratioToBaselineAtFinest: 0.8
  arms: [shear]
