# UNIFORM TWINS of the refined ladder (config/stationaryDroplet3Drefined.yaml) at
# N = 60/76/96 on the 6R box, RE-RUN on the current code: config/stationaryDroplet3Dwide
# was biharmonicBand-filtered (theta 0.2) and predates the explicit one-field viscous
# term and the alg_lin face viscosity, so its numbers are not comparable. 96 rather than
# the old 95 so the twin matches the refined arm exactly. Production chain, no filter.
# 216000 / 438976 / 884736 cells; np 32 constant across the arms.
study_name: stationaryDroplet3Duniform
case: stationaryDroplet3D
mesh: hex
mode: parallel
np: 32
solver: leiaSemiLagrangianLevelSetTwoPhaseFoam
theme:  semi-lagrangian-level-set
setfields_args: "-alphaName alpha.water"
solve_runtime: 900
axes_override:
  DOMAIN_LENGTH: [0.006]
  N_CELLS: [60, 76, 96]
  END_TIME: [0.025]
  WRITE_INTERVAL: [0.0125]
  SURFACE_TENSION_FORCE: [reconstructedCurvature]
  CURVATURE_EXTENSION: [cellCentreInverse]
  FACE_CURVATURE_SOURCE: [model]
  PSI_FILTER: [none]
  VOLUME_CORRECTION: [noVolumeCorrection]
  MOMENTUM_DDT_SCHEME: [backward]
  RHO_DDT_SCHEME: [backward]
  MOMENTUM_DIV_SCHEME: [upwind]
  MASS_FLUX: [rhoLENT]
  MASS_FLUX_BOUND_RHO: ["true"]
  PHASE_INDICATOR: [detrixheAslam]
  SL_TRACE_VELOCITY: [projectedFlux]
  SL_RECONSTRUCTION: [uncachedQuadraticWeightedLeastSquares]
  SL_FOOT_INTEGRATOR: [taylor]
collapse_other_axes: true
export_slides: false
mpi_launcher: "mpirun -np {np}"
env_preamble: "source $HOME/OpenFOAM/OpenFOAM-v2512/etc/bashrc; [ -f $HOME/.leia_env ] && . $HOME/.leia_env || true"
