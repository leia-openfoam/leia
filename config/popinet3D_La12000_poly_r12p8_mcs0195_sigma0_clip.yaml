# SI UNITS (2026-09-08). This study runs Popinet's benchmark in real units at his own
# dimensionless groups: D = 1 mm, rho = 1000 kg/m^3 and nu = 1e-6 m^2/s in BOTH phases,
# sigma = 0.012 N/m, U = 0.0692820323 m/s, box 5 x 2.5 (x 2.5) mm, T_U = D/U = 0.01443375673 s.
# That gives La = sigma D/(rho nu^2) = 12000, We = rho U^2 D/sigma = 0.4, Re = 69.28, and
# density and viscosity ratios of 1 -- so every number Popinet reports still applies.
# Sentences below that use his units (D = 0.4, U = 1, sigma = 1, the unit square, 0.13107)
# describe the SAME case: multiply a length by 2.5e-3 m, a velocity by 0.0692820323 m/s and
# a time by 0.03608439182 s. Verify with:
#     python3 workflow/scripts/popinet_si.py check <this file>
#
# QUASI-MONOTONE CLIP GATE (2026-09-08). The sigma-0 control proved the polyhedral far-field failure
# is the semi-Lagrangian transport ALONE: with an exactly uniform stream (max|u'| 2e-14 for 1563
# steps) the level set still developed the fake zero set at step 508 -- the same step as with
# surface tension -- and reached errors of 7e4 by T = 0.4 (61 335 cells across zero). A point-value
# SL update with a least-squares fit amplifies a checkerboard error at ~ (U dt / h) x stencil
# asymmetry per step; cfMesh's small boundary cells are the asymmetric ones. The candidate remedy
# is the quasi-monotone clip of the reconstructed value to the stencil's bounds
# (clipToStencilBounds, existing, off by default in the two-phase templates). MEASURED in 2D N = 64
# (hex) before submission: the clip is NOT inert at the interface -- L1/L2 |u'| -6 % / -5 %,
# volume error +30 %, shape +5 %, centroid +12 % -- so a global clip is a method change; the
# clean form is a band-aware clip (far field only), to be implemented only if this arm passes.
# THIS ARM: SIGMA 0 (passive level set, exactly uniform stream) + global clip. PRE-REGISTERED: PASS = no
# zero-set jump to T = 0.4 and far-field |psi - psi_exact| bounded; FAIL = the clip does not stabilise.
study_name: popinet3D_La12000_poly_r12p8_mcs0195_sigma0_clip
case: popinetTranslating3D
mesh: poly
mode: parallel
np: 32
solver: leiaSemiLagrangianLevelSetTwoPhaseFoam
theme:  method-comparison
setfields_args: "-alphaName alpha.water"
solve_runtime: 300
axes_override:
  SL_CLIP: ["true"]
  N_CELLS: [64]
  MAX_CELL_SIZE: [4.875e-05]
  REFINE_ALLOW_PIN_MISMATCH: ["true"]
  END_TIME: [0.01443375673]
  WRITE_INTERVAL: [0.007216878365]
  POPINET_NX: [128]
  POPINET_XLEN: [2]
  DOMAIN_LENGTH: [0.0025]
  CAPILLARY_REF_LENGTH: [0.0025]
  CAPILLARY_DT_COEFF: [0.004729265146]
  DROPLET_RADIUS: [0.0005]
  DROPLET_OFFSET_X: [0]
  TRANSLATION_SPEED: [0.0692820323]
  SIGMA: [0]
  POPINET_RHO: [1000]
  POPINET_NU: [1.0e-06]
  SURFACE_FILE: [box5x2p5x2p5mm.fms]
  MOMENTUM_DIV_SCHEME: ["upwind"]
  MASS_FLUX: ["rhoLENT"]
  MASS_FLUX_BOUND_RHO: ["true"]
  VOLUME_CORRECTION: ["noVolumeCorrection"]
  MOMENTUM_DDT_SCHEME: ["backward"]
  RHO_DDT_SCHEME: ["backward"]
  CURVATURE_EXTENSION: ["none"]
  SURFACE_TENSION_FORCE: ["reconstructedCurvature"]
  FACE_CURVATURE_SOURCE: [model]
  PSI_FILTER: [none]
  PHASE_INDICATOR: [detrixheAslam]
  SL_TRACE_VELOCITY: ["projectedFlux"]
  SL_RECONSTRUCTION: [uncachedQuadraticWeightedLeastSquares]
  SL_FOOT_INTEGRATOR: ["taylor"]
collapse_other_axes: true
export_slides: false
mpi_launcher: "mpirun -np {np}"
env_preamble: "source $HOME/OpenFOAM/OpenFOAM-v2512/etc/bashrc; [ -f $HOME/.leia_env ] && . $HOME/.leia_env || true"
